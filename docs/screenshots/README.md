# Screenshots

Drop captures here and reference them from the top-level [README](../../README.md).

Suggested set:

- `popover.png` — menu-bar popover with a few demo accounts (use throwaway QRs from <https://stefansundin.github.io/2fa-qr/> so you don't leak real secrets)
- `import.png` — the import window
- `export.png` — the export window with generated URIs
- `settings.png` — the settings sheet showing the Open-at-login toggle

Use macOS's *Markup* to round the corners and hide private data before
committing.
